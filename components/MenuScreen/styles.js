import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    screen: {
    },
    buttonContainer : {
        position: "absolute",
        width:"100%",
        top: 520
    }
});

export default styles;