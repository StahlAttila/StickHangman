import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    listArea: {
      
    },
    list: {
       backgroundColor: "red",
       height: 600
    }
});

export default styles;